/**
 * @description 现存活动的接口
 * @interface ExistingActivity
 * @property {string} name - 活动名称
 * @property {string} packet - 数据包
 */
export interface ExistingActivity {
  name: string;
  packet: string;
}
