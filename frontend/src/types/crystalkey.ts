/**
 * @description 晶钥的接口
 * @interface CrystalKeyType
 * @property {number} id - 晶钥 ID
 * @property {string} name - 晶钥名称
 * @property {string} desc - 描述
 * @property {string} color - 颜色
 */
export interface CrystalKeyType {
  id: number;
  name: string;
  desc: string;
  color: string;
}
